function solution(n) {

    let odd = [];
    
    for(let i=0; i<=n; i++){
        if(i%2===1){
            odd.push(i);
        }else{
            continue;
        }
    }
    odd.sort((a,b)=>a - b);
    return odd;
}