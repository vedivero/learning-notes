function solution(n, k) {
    
    let serviceDrink = Math.trunc(n / 10);
    let totalLambPrice = n * 12000;
    let totalDrinkPrice = k * 2000;
    
    let totalPrice = totalLambPrice + totalDrinkPrice;
    
    if(n > 0) return totalPrice - (serviceDrink * 2000);
    else totalPrice;
    
}